metadata:
  version: 1

description: Master catalog

sources:

  hindcast:
    description: Hindcast testing catalog
    driver: intake.catalog.local.YAMLFileCatalog
    args:
      path: "{{CATALOG_DIR}}/hindcast/hindcast.yml"
    metadata: {}
