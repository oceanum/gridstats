build_image:
  image: docker:26.1.3
  services:
  - docker:26.1.3-dind
  script:
    - docker login -u gitlab-ci-token -p $CI_JOB_TOKEN registry.gitlab.com
    - docker-compose build
    - docker-compose push
  only:
    - tags
    - master
